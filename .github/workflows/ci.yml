name: CI
on:
  push:
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - '.gitignore'
      - '.vscode/**'
    tags:
      - v*
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - '.gitignore'
      - '.vscode/**'

env:
  CARGO_TERM_COLOR: always
permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup rust cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "${{ runner.os }}-tosho-rust"

      - name: Build
        run: cargo build --verbose --all

      - name: Lint
        run: cargo clippy --verbose --all -- -D warnings

      - name: Tests
        run: cargo test --verbose --all

  build:
    needs: lint
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
      fail-fast: false
    
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup rust cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "${{ runner.os }}-tosho-build-rust"

      - name: Build
        run: cargo build --release --verbose --all

      - name: Rename binary (non-windows)
        if: matrix.os != 'windows-latest'
        run: mv target/release/tosho target/release/tosho-${{ runner.os }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tosho-${{ runner.os }}
          path: target/release/tosho*

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: |
            target/release/tosho*
          draft: true
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
          generate_release_notes: true
